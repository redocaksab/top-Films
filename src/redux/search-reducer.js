let initialStore = {
    films: []
};


const searchReducer = (store = initialStore, action) => {
    switch(action.type) {
        default:
            return store;
    }
}


export default searchReducer;